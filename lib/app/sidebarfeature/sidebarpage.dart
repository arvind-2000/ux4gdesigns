import 'package:flutter/material.dart';

class SideBarPages extends StatelessWidget {
  const SideBarPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("SideBar", style: TextTheme.of(context).headlineLarge));
  }
}
